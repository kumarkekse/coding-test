<% @product_items.each do |product_item| %>
  <tr>
    <th scope="row"><%= product_item.item.id %></th>
    <td><%= product_item.item.name %></td>
    <td><%= product_item.product.name %></td>
    <td><%= product_item.sold_quantity %></td>
  </tr>
<% end %>